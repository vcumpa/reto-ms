FROM node:20-alpine AS build
WORKDIR /app

# Esta capa solo cambia cuando cambia el lockfile; Docker puede reutilizarla
# para no descargar dependencias en cada modificación del código fuente.
COPY package*.json ./
RUN npm ci --no-audit --no-fund

COPY . .
RUN npm run build

FROM nginx:1.27-alpine
COPY --from=build /app/dist /usr/share/nginx/html
COPY nginx.conf /etc/nginx/conf.d/default.conf
EXPOSE 80
